// {{$name}}.swift
// This code was generated by codegen.js (https://github.com/alexdoloz/swift-telegram-bot-api-codegen) 
// Date: {{{currentDate}}}
// Please don't edit this file manually since it can be overwritten at any moment.


import Foundation


public struct {{$name}}{{protocols protocols}} {
    {{#each props}}
    public var {{nameMap @key this.name}}: {{this.type}}
    {{/each}}

    public init(
        {{#each props}}
        {{{initParam (nameMap @key this.name) this}}}{{#unless @last}},{{/unless}}
        {{/each}}
    ) {
        {{#each props}}
        self.{{nameMap @key this.name}} = {{nameMap @key this.name}}
        {{/each}}
    }
}


public extension {{$name}} {
    public enum CodingKeys: String, CodingKey {
        {{#each props}}
        case {{nameMap @key this.name}} = "{{@key}}"
        {{/each}}
    }
}